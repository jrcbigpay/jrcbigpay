SELECT
  DATE_TRUNC(created_at, MONTH) AS reg_month,
  COUNT(DISTINCT user_id)
FROM
  `cormac-sandbox.mys_models_a.user_summary_table`
WHERE
  user_status = 'REGISTERED'
  AND first_topup IS NULL
GROUP BY 1 
ORDER BY 1 DESC
